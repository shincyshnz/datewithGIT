** git branch 'branch name' **

eg: git branch feature 

-- to move to the feature branch we can use the following command

** git checkout feature **

the commit done in one branch donot reflect in another
.........

MERGE
-------------------
3 types of merge are there

    1) Fast forward merge
    --------------------------

        A base branch will be there (for eg: master) and a new branch(eg:feature). then we checkout to base bracnh(master) and merge the other(feature).


